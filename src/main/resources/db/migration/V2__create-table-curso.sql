CREATE TABLE curso_forum (
                               id_curso BIGINT AUTO_INCREMENT PRIMARY KEY,
                               nome_curso VARCHAR(255) NOT NULL,
                               categoria_curso VARCHAR(255) NOT NULL
);
